# Contributing

Documentation should describe configurations that have actually been
validated.

For a new component, record:

-   exact version;
-   storage location;
-   associated toolchain;
-   environment loader;
-   relevant environment variables;
-   build and install commands;
-   compatibility patches;
-   validation or smoke-test procedure.

Do not describe unvalidated library combinations as supported
configurations.
